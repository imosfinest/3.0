// math methods
let price = 109839037340947490393039;
const foo = Math.random(price) * 100000000;
console.log(foo);

const pin = Math.floor(foo);
console.log(pin);

// modulo
const age = 30;
let x = 20;
let y = 3;

let sum = x % y;
console.log(sum);

console.log(`This is a random number ${foo} and this is the pin, ${pin} `);
